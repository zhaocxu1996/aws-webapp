version: 0.0
os: linux

files:
  - source: ./target/ROOT.war
    destination: /home/ubuntu/webapps

hooks:
  AfterInstall:
    - location: ./codedeploy/afterInstall.sh
      timeout: 180
      runas: ubuntu